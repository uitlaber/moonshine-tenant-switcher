<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Модель тенанта (проекта)
    |--------------------------------------------------------------------------
    |
    | Eloquent-модель, экземпляры которой выбираются в переключателе. В этом
    | проекте — App\Models\Site. Без неё переключатель не отображается, а
    | глобальный скоуп не применяется.
    |
    */
    'tenant_model' => null,

    /*
    |--------------------------------------------------------------------------
    | Внешний ключ тенанта на скоупящихся моделях
    |--------------------------------------------------------------------------
    |
    | Колонка, по которой фильтруются модели с трейтом BelongsToTenant
    | (например, `site_id`). Можно переопределить на конкретной модели
    | свойством `protected $tenantForeignKey`.
    |
    */
    'foreign_key' => 'tenant_id',

    /*
    |--------------------------------------------------------------------------
    | Отображение списка тенантов
    |--------------------------------------------------------------------------
    */
    'label_column' => 'name',
    'active_column' => null,   // если задано — в списке только where(active_column, true)
    'order_column' => 'id',

    /*
    |--------------------------------------------------------------------------
    | Логотип тенанта в переключателе
    |--------------------------------------------------------------------------
    |
    | logo_column — атрибут (можно dot-путь, напр. settings.logo), где лежит
    | путь к мини-логотипу. Если пусто или у тенанта нет лого — показывается
    | бейдж с первой буквой названия.
    | logo_url_prefix — префикс к относительному пути (напр. /storage). Если
    | значение уже абсолютный URL или начинается с /, префикс не добавляется.
    |
    */
    'logo_column' => null,
    'logo_url_prefix' => '',

    /*
    |--------------------------------------------------------------------------
    | Сессия и guard
    |--------------------------------------------------------------------------
    |
    | session_key — ключ, в котором хранится id текущего тенанта.
    | guard — guard, при аутентификации под которым активируется скоуп
    | (по умолчанию moonshine: скоуп работает только внутри админки).
    |
    */
    'session_key' => 'moonshine_tenant_id',
    'guard' => env('MOONSHINE_AUTH_GUARD', 'moonshine'),
];
